<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- H2 In-Memory Database named movietheaterdb -->
        <property name="hibernate.connection.driver_class">org.h2.Driver</property>
        <property name="hibernate.connection.url">jdbc:h2:mem:movietheaterdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE</property>
        <property name="hibernate.connection.username">sa</property>
        <property name="hibernate.connection.password"></property>

        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!--
            hbm2ddl.auto = update:
            - First run  → CREATE tables
            - Next runs  → UPDATE schema (add new columns), never drop existing data
            Use 'create' only during initial dev when you want a clean slate each time.
        -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Entity mappings -->
        <mapping class="fa.training.entities.CinemaRoom"/>
        <mapping class="fa.training.entities.CinemaRoomDetail"/>
        <mapping class="fa.training.entities.Seat"/>
    </session-factory>
</hibernate-configuration>
