<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ============================================ -->
        <!-- DATABASE CONNECTION SETTINGS (PostgreSQL)   -->
        <!-- ============================================ -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/fadb</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">postgres</property>

        <!-- ============================================ -->
        <!-- DIALECT                                     -->
        <!-- ============================================ -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- ============================================ -->
        <!-- SHOW SQL QUERIES (for debug)                -->
        <!-- ============================================ -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ============================================ -->
        <!-- DDL AUTO                                    -->
        <!-- create  : drop & recreate tables on start  -->
        <!-- update  : only alter tables if needed      -->
        <!-- validate: check schema, no changes         -->
        <!-- none    : do nothing                       -->
        <!-- ============================================ -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Connection pool size -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- ============================================ -->
        <!-- MAPPED ENTITY CLASSES                       -->
        <!-- ============================================ -->
        <mapping class="fa.training.entities.Employee"/>

    </session-factory>
</hibernate-configuration>
