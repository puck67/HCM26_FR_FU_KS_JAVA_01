<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">org.h2.Driver</property>
        <property name="connection.url">jdbc:h2:mem:trainingdb;DB_CLOSE_DELAY=-1</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Format the generated SQL -->
        <property name="format_sql">true</property>
        <property name="highlight_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <!-- Using update for this exercise as per task 1 requirements -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapped entity classes -->
        <mapping class="fa.training.entity.Student"/>
        <mapping class="fa.training.entity.Course"/>

    </session-factory>
</hibernate-configuration>
