spring.application.name=demo

# H2 Database Configuration
# Uses a file-based path to persist data long-term and avoid memory loss upon application stop
spring.datasource.url=jdbc:h2:file:./data/lmsdb;AUTO_SERVER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=securepassword123
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Hibernate/JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
# Keeps schema updated without deleting or dropping tables during start/restart
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
